we have createc index.js coz whenever we create components inporting and exporting becomes messy inside the component's files so we import all the reusable components and expoet whenever we needed ,we just have to import index.js and can export whichever the file we need from this only one file so the hassel of imporitnf  and exporting every components to one another then the code becomes complicated .